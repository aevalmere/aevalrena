# Reference art

Drop the character reference sheet here as `aeval-ref.png` (the Water Mage sheet with front, side,
back views and the idle, walk, run, jump, fall, land, hit, taunt strips). Workers read the palette and
silhouette from `docs/SPEC.md` section 7, which was written from that sheet, so the PNG is for humans.
